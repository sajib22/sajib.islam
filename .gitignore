# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*

# Secrets — nothing with a credential in it belongs in this public repo
.env
.env.*
*.pem
*.key

# Dependencies (this project has none, and should stay that way)
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
